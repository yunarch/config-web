/**
 * Add two numbers. It shouldn't break when using english contractions.
 *
 * @param a - first parameter to check.
 * @param b - second parameter to check.
 * @returns The sum of a and b.
 */
export function add(a: number, b: number) {
  return a + b;
}
